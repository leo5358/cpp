#include<bits/tdc++.h>
#define fin ios::sync_with_stdio(0);cin.tie(0);
using  namespace std;
int main()
{
    vector<string>v;
    string s;
    while(getline(cin,s))
    {
        if(a=="ADD")
        {
            v.push_back(string s);
        }
        else if(a=="INSERT")
        {
            find(v.begin(),v.end(),string s);
            v.insert(auto it,string s) 
        }
        else if(a=="REMOVE")
        {
            v.erase(auto it);
        }
    }


}