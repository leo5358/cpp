#include<bits/stdc++.h>
#define fine ios_base::sync_with_stdio(0);cin.tie(0);
using namespace std;
int main()
{
    fine
    double a,b;
    cin>>a;
    cin>>b;
    cout<<fixed<<setprecision(1)<<a/pow(b,2)<<endl;
}